AIra/
│
├── aira/
│   ├── main.py             
|   ├── test_loader.py  
|   ├── build_faiss.py
│   │
│   ├── agents/
│   │   ├── base_agent.py
│   │   ├── tool_agentpy
│   │
│   ├── api/
│   │   ├── agent.py
│   │   ├── chat.py
│   │   ├── rag.py
│   │   ├── health.py
│   │   ├── documents.py
│   │   └── rerank.py
│   │
│   ├── core/
│   │   ├── llm_loader.py
│   │   ├── config.py
│   │   ├── dependencies.py
│   │   └── prompt_manager.py
│   │
│   ├── chains/
│   │   ├── basic_chain.py
│   │   └── rag_chain.py
│   │
│   ├── rag/
│   │   ├── bm25_retriever.py
│   │   ├── embeddings.py
│   │   ├── hybrid_reriever.py
│   │   ├── kb_metadata.py
│   │   ├── loader.py
│   │   ├── reranker.py
│   │   ├── retriever.py
│   │   ├── semantic_chunker.py
│   │   ├── summarizer.py
│   │   ├── vectorstore.py
│   │   └── document_loader.py
│   │
│   ├── tools/
│       ├── calculator.py
│       ├── search.py
│       └── rag_tool.py
│
└── data/
│   ├──faiss/
│   ├──kb_metadata.json
│   ├──uploads_metadata.json


=========================================================
Layer	                Responsibility                  |
------                 ----------------                 |
LLM	                    Generate text                   |
Chain	                How to answer                   |
RAG	                    Retrieve + answer               |
Agent	                Decide which chain to use       |
=========================================================

- [x] Week 1: Core LLM Inference & Modular Architecture 
- [ ] Week 2: FastAPI-based Serving Layer(main.py should not call the llm directly)
   📌 What We Are NOT Doing in Week-2
    ❌ Auth
    ❌ RAG
    ❌ Memory
    ❌ Agents
    ❌ MLflow
    Version	Meaning
    0.1.0	Core LLM inference (Week 1)
    0.2.0	API serving layer (Week 2)
- [ ] Week 3: RAG with Vector Stores + DVC
        CrossEncoder reranking, bm25, hybrid search
- [ ] Week 4: Agentic AI with Tools & Memory 
- [ ] Week 5: LoRA Fine-Tuning + MLflow 
- [ ] Week 6: QLoRA + Optimization 
- [ ] Week 7: vLLM Deployment 
- [ ] Week 8: Docker + CI/CD 
- [ ] Week 9+: Cloud Deployment & Monitoring


##### STEP-1 ######

* we use __init__.py file --> code in it run first as soon as the package is imported
* from pathlib import Path --> Imports a modern way to handle filesystem paths (better than strings).
* BASE_DIR = Path(__file__).parent.parent --> goes two levels back from the current script
* MODEL_DIR.mkdir(exist_ok=True, parents=True) --> parents=True makes sure the the parents dir exist
* we use @staticmethod inside class for any method in the class so that when we can use the 
  method directly withiout making any object from the class. it does not take self as a inut inside
  class, so it cannot interact with other methods or attributes inside class.
  e.g.--> prompt = PromptManager.get_basic_chat_prompt()
* What do you mean by prompt abstraction?
  In AIra, prompts are treated as first-class components—defined independently from model 
  inference and orchestrated via LangChain PromptTemplates—so they can evolve with RAG, 
  agents, and fine-tuning without changing core logic


our current position(sha id: b1d656d-->we are getting hellusinated and unstructured outputs from 
                    the model)
    Raw LLM generation
    ↓
    Unbounded output
    ↓
    Hard to control
    ↓
    Not production-safe

    1. we are using Qwen/Qwen3-0.6B which is a text generation model
    2. raw inference, model don't know when to stop
    Raw inference ≠ Production inference


our current position(sha id: d67d6a0-->we are getting proper output with <think> as we are using
                    the template on which the qwen was trained on)
    Tokenizer-aware LLM abstraction
        ↓
    Qwen-native chat prompt alignment
        ↓
    LangChain BasicChain orchestration
        ↓
    EOS-driven bounded generation
        ↓
    Clean, deterministic responses




 
    *currently our model is CLI(commad line interface) only
    * dependencies.py (ask why we need this)
        Later you can:
            Cache the model
            Swap models
            Add RAG chains
            Mock in tests


##### STEP-3 ######
    RAG plan
        Hybrid Search                       Week 4
        Re-ranking                          Week 4
        Query Decomposition                 Week 5
        HyDE                                Week 5
        Parent Retrieval                    Week 3
        Self-Querying                       Week 6

    *in rag we keep the model for emmbeding model to be small unlike the main llm model
    🔜 Next step (choose one — ONE at a time) 
    1️⃣ PDF ingestion pipeline (PyPDFLoader → chunk → FAISS) 
    2️⃣ BM25 + Hybrid Search 
    3️⃣ DVC setup for RAG datasets 
    4️⃣ RAG evaluation hooks



    ✅ Week 1 – Solidify Current RAG (NOW)

Goal: Make sure RAG works correctly and is observable.

Tasks

Verify documents are actually retrieved (log chunk text + source)

Replace deprecated .run() → .invoke()

Add top_k configuration

Log:

Retrieved chunk count

Chunk sources

Response latency

Handle empty retrieval case gracefully

Clean prompt and context formatting

📌 Done when:

You can explain why a particular answer was generated

✅ Week 2 – Improve Retrieval Quality

Goal: Better answers without adding complexity.

Tasks

Add CrossEncoder re-ranking

FAISS top 20 → rerank → top 5

Compare answers before/after reranking

Make reranking optional via env flag

Add score logging

📌 Done when:

Fewer irrelevant chunks

More focused answers

✅ Week 3 – Query Understanding

Goal: Handle vague or short questions better.

Tasks

Add lightweight query expansion

Single expanded query only

Toggle via config

Log original vs expanded query

📌 Done when:

Short questions retrieve relevant context

✅ Week 4 – Dataset & Index Versioning

Goal: Make the pipeline reproducible.

Tasks

Track PDFs, chunks, FAISS index with DVC

Version ingestion and index builds

Add make ingest and make build_index

Document dataset versions

📌 Done when:

You can rebuild the same index anytime

✅ Week 5 – Simple Agent Routing

Goal: Decide when to use RAG.

Tasks

Add a small router:

factual → RAG

conversational → LLM

Two tools only:

rag_search

llm_answer

Log routing decisions

📌 Done when:

Not every query blindly hits the vector DB

✅ Week 6 – Multi-Query (Optional)

Goal: Improve recall for ambiguous queries.

Tasks

Add multi-query retrieval (max 3 variants)

Deduplicate results

Agent decides when to use it

📌 Skip if current results are good

✅ Week 7 – Model Improvement

Goal: Improve answer quality.

Tasks

Fine-tune LLM using LoRA

Focus on:

factual tone

citation usage

Track experiments with MLflow

📌 No embedding fine-tuning yet

✅ Week 8 – Optimization

Goal: Reduce cost and latency.

Tasks

Quantize model (QLoRA)

Cache embeddings and expanded queries

Dynamic context trimming

✅ Week 9 – Production Readiness

Goal: Deployable system.

Tasks

vLLM serving

Streaming responses

Dockerize app

Add health checks

✅ Week 10 – Monitoring

Goal: Observe and trust the system.

Tasks

Log retrieval confidence

Alert on empty retrieval

Add basic dashboards


